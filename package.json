{
  "name": "routineflow",
  "version": "1.0.0",
  "description": "RoutineFlow — Build your best day, every day.",
  "scripts": {
    "build": "echo 'Static HTML app — no build step needed'",
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0",
    "@capacitor/local-notifications": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
